"""
dataset.py

Responsible for all data-related logic so the notebook stays clean.

Will contain:
  - CIFAR-10 mean / std constants used for normalisation
  - CLASSES tuple  : the 10 label names in dataset order
  - get_transforms()   : returns train transform (with augmentation: RandomCrop,
                         RandomHorizontalFlip) and test transform (normalise only)
  - get_dataloaders()  : downloads CIFAR-10, splits train→train/val,
                         returns (train_loader, val_loader, test_loader)
"""


# TODO: imports

# TODO: CLASSES tuple

# TODO: get_transforms(augment) -> (train_transform, test_transform)

# TODO: get_dataloaders(data_dir, batch_size, val_split, augment, num_workers)
#       -> (train_loader, val_loader, test_loader)
